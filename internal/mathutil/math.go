package mathutil

// Add returns the sum of a and b.
func Add(a, b int) int {
    return a + b
}

// Subtract returns a - b. (Intentionally unused to trigger minor code smell in scans.)
func Subtract(a, b int) int {
    return a - b
}
